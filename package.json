{
  "name": "galileo-minecraft-bot",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "gen": "wrangler types && node src/scripts/gen-handlers.mjs",
    "deploy": "npm run gen && wrangler deploy"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "wrangler": "^4.45.0"
  }
}
